import java.util.ArrayList;

public class Statics extends Position
	{
	public static void stats(ArrayList<Integer> kills, ArrayList<Integer> attempts, ArrayList<Integer> errors)
		{
		// calculate the stats
		// (kills - errors)/ attempts
		//example:
		//int stats = (int) (((kills - errors) / attempts) * 1000);
		//System.out.println(stats);
		}

	}
